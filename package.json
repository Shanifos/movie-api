{
  "name": "movie-api",
  "version": "1.0.0",
  "description": "## Instructions In the initial commit to this project, you have been provided with a CSV file that contains data on the American Film Institute's (AFI) Top 100 Movies. Your task is to create a database backed Express API for access the details of these films.",
  "main": ".eslintrc.js",
  "scripts": {
    "dev": "npm run migrate:dev && npm run server:dev",
    "migrate:dev": "./node_modules/.bin/env-cmd sequelize db:migrate --env development --config ./config/sequelize.js",
    "server:dev": "./node_modules/.bin/env-cmd nodemon index.js",
    "migrate:prod": "sequelize db:migrate --env production --config ./config/sequelize.js",
    "server:prod": "node index.js",
    "start": "npm run migrate:prod && npm run server:prod"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Shanifos/movie-api.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Shanifos/movie-api/issues"
  },
  "homepage": "https://github.com/Shanifos/movie-api#readme",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mysql2": "^2.0.1",
    "sequelize": "^5.21.2",
    "sequelize-cli": "^5.5.1"
  },
  "devDependencies": {
    "env-cmd": "^10.0.1",
    "nodemon": "^2.0.1"
  }
}
